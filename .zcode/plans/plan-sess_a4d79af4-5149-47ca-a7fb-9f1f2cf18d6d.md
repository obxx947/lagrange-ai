# 拉格朗日智能体 — 局域网部署与环境检查计划（修订）

## 新增约束：所有文件目录嵌套深度 ≤ 10层
- 现有的 webpack 别名路径（如 `@lagrange/core`）将扁平化映射
- `src/` 目录结构限制在最多3-4层嵌套
- 不创建深层嵌套的组件目录树

---

## 一、环境检查结果总览

### ✅ 正常运行
- Python 3.12.4：所有14个依赖包已安装，FastAPI运行在端口3000
- 后端健康：169艘舰船数据完整，FAISS向量索引已构建
- SQLite数据库：5张核心表正常（3用户/4聊天记录/2模拟保存）

### ⚠️ 需要修复
| 问题 | 严重程度 |
|------|----------|
| 前端依赖缺失（35+包未安装） | 高 |
| 服务器绑定127.0.0.1，局域网无法访问 | 高 |
| 数据库Schema不完整 | 中 |
| 备份未启用 | 中 |
| src/和public/目录缺失 | 中 |

---

## 二、执行步骤

### 步骤1：服务器监听地址修改
- 修改 `main.py`：uvicorn绑定从`127.0.0.1`改为`0.0.0.0`
- 同步修改 `manage.ps1` 和 `manage.sh`

### 步骤2：Windows防火墙配置
- `netsh advfirewall` 开放端口3000（TCP入站）

### 步骤3：修复前端环境
- 重写 `package.json`：添加webpack需要的所有依赖和scripts
- `npm install` 安装依赖
- 创建 `src/` 目录（最多2-3层嵌套，符合≤10层约束）
- 确保webpack输出到 `static/dist/`（现有static目录深度仅1层）

### 步骤4：更新前端API地址
- 修改 `static/index.html` 中硬编码地址为相对路径或局域网IP

### 步骤5：数据库补全
- 执行 `db/schema.sql` 中缺失的DDL

### 步骤6：备份配置
- 使用 scheduler.py 设置定时备份

### 步骤7：全文件验证
- 逐一检查25个Python源文件
- 检查所有配置文件一致性
- 验证目录嵌套深度不超过10层

### 步骤8：重启服务并局域网验证
- 重启服务监听0.0.0.0:3000
- 从局域网另一台机器测试访问