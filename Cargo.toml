[package]
name = "lagrange-health"
version = "0.1.0"
edition = "2021"
description = "拉格朗日AI — Rust 健康检查工具"

[dependencies]
reqwest = { version = "0.12", features = ["blocking"] }
tokio = { version = "1", features = ["full"] }
