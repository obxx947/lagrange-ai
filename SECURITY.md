# 拉格朗日智能体 - 安全策略
# Security Policy

## 支持的版本

| 版本 | 支持状态 |
|------|---------|
| 2.0.x | ✅ 活跃支持 |
| 1.0.x | ⚠️ 安全修复 |
| < 1.0 | ❌ 不再支持 |

## 报告漏洞

如果你发现安全漏洞，请**不要**提交公开Issue。

### 报告流程
1. 发送邮件至: security@lagrange-agent.example.com
2. 标题包含 `[SECURITY]` 前缀
3. 详细描述漏洞:
   - 受影响版本
   - 复现步骤
   - 潜在影响
   - 建议修复方案

### 响应时间
- 确认收到: 48小时内
- 初步评估: 5个工作日内
- 修复发布: 根据严重程度

### 漏洞严重程度

| 级别 | 说明 | 修复时间 |
|------|------|---------|
| Critical | 远程代码执行、未授权数据访问 | 72小时 |
| High | 权限提升、敏感信息泄露 | 1周 |
| Medium | XSS、CSRF、SSRF | 2周 |
| Low | 信息泄露（非敏感）、配置问题 | 下个版本 |

## 安全最佳实践

### API密钥管理
- DeepSeek API密钥通过环境变量注入，不硬编码
- JWT密钥使用随机生成的64字符字符串
- 管理员密码在生产环境必须修改

### 数据保护
- 用户密码使用 bcrypt (cost=12) 哈希存储
- JWT令牌7天自动过期
- 敏感日志不包含完整API密钥

### 访问控制
- 管理接口仅允许 127.0.0.1 访问
- 用户API需要JWT认证
- 频率限制: 10请求/小时/用户

### 依赖安全
- 定期更新Python依赖 (`pip list --outdated`)
- 使用 `bandit` 进行静态安全分析
- Docker镜像使用非root用户运行

## 披露政策

漏洞修复后，我们将在CHANGELOG中披露，并在修复发布后30天内发布详细安全公告。
